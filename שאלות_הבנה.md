# תשובות לשאלות הבנה – React

**1. איך אפשר לתת ערכי ברירת מחדל ל-props של קומפוננטת פונקציה? האם נבצע זאת תמיד? מדוע?**
* **איך מגדירים:** 
  * בעזרת Default Parameters בתוך ה-Destructuring של הפרמטרים: `function MyComponent({ color = 'red' })`
  * או בעזרת תנאי מקוצר בגוף הפונקציה: `const finalColor = color || 'red'`
* **האם תמיד ונסיבות:** לא נבצע זאת תמיד. נשתמש בערכי ברירת מחדל רק כאשר ה-prop אינו חובה (אופציונלי), כדי למנוע שגיאות בזמן ריצה ולספק תצוגה תקינה גם אם המשתמש לא העביר את הנתון.

---

**2. מהו JSX?**
* **JSX (JavaScript XML)** הוא הרחבת תחביר (Syntax Extension) של JavaScript המאפשרת לכתוב מבנה דמוי HTML ישירות בתוך קוד JavaScript. הוא מקל על כתיבת הממשק ומקומפל בסופו של דבר לקריאות פונקציה רגילות של React (`React.createElement`).

---

**3. מה צריך להיות בתחביר camelCase בריאקט? (3)**
1. **שמות מאפיינים ותכונות (Props & HTML Attributes):** כגון `className`, `tabIndex`, `readOnly`.
2. **מטפלי אירועים (Event Handlers):** כגון `onClick`, `onChange`, `onSubmit`, `onMouseEnter`.
3. **מפתחות עיצוב ב-Inline Style:** כגון `backgroundColor`, `fontSize`, `flexDirection`.

---

**4. מה משותף בין התכונות class ו-for ב-HTML? האם ניתן להשתמש בהם ב-JSX?**
* **המשותף:** שתיהן מילות שמורות (Reserved Keywords) בשפת JavaScript.
* **שימוש ב-JSX:** לא ניתן להשתמש בהן בצורתן המקורית ב-JSX. במקומן משתמשים בתחביר התואם:
  * `className` במקום `class`
  * `htmlFor` במקום `for`

---

**5. מה זה js expression והיכן נשתמש בו ב-JSX?**
* **הגדרה:** ביטוי JavaScript שמחשב ומחזיר ערך (כגון משתנה, חישוב מתמטי, קריאה לפונקציה, או תנאי מקוצר/טרנרי).
* **שימוש ב-JSX:** משתמשים בו בתוך סוגריים מסולסלים `{}` – הן עבור תוכן פנימי של תגיות והן עבור ערכים של מאפיינים (Attributes).

---

**6. איך ריאקט מזהה האם התגית היא תגית רגילה ב-HTML או קומפוננטת ריאקט?**
* לפי אות הרישיות הראשונה של שם התגית:
  * **אות קטנה (lowercase):** מזוהה כתגית HTML סטנדרטית (כגון `div`, `span`, `h1`).
  * **אות גדולה (Capital letter):** מזוהה כקומפוננטת React מותאמת אישית (כגון `<Flower/>`, `<Header/>`).

---

**7. מה ההבדל בין הערך שניתן להכניס בתוך `{}` שהוא תוכן של תגית, לערך כאשר מדובר על תוכן של תכונה?**
* **תוכן של תגית (Children):** מיועד להצגה על המסך. יכול לקבל טקסטים, מספרים, או אלמנטים של JSX (ערכים כמו `boolean`, `null`, או `undefined` פשוט לא ירונדרו).
* **תוכן של תכונה (Attribute):** מעביר נתון ישיר לרכיב. יכול לקבל כל טיפוס נתונים ב-JS (פונקציות, אובייקטים, מערכים, ערכים בוליאניים וכו') לצורך הגדרת התנהגות או סגנון.

---

**8. מה נעשה כאשר נרצה להחזיר מספר תגיות מתוך קומפוננטה? (2 אפשרויות)**
1. עטיפת כל התגיות בתוך תגית עוטפת יחידה מ-HTML (כגון `<div>` או `<section>`).
2. עטיפת התגיות ב-Fragment שקוף (כגון `<>...</>` או `<React.Fragment>...</React.Fragment>`).

---

**9. מהי התגית השקופה Fragment? מהם הדרכים לכתוב אותה? (3) האם ניתן לשים עליה תכונות?**
* **מהי:** רכיב מיוחד המאפשר לקבץ רשימת אלמנטים ללא הוספת אלמנט DOM מיותר לרנדור.
* **3 דרכים לכתיבה:**
  1. תחביר מקוצר: `<>...</>`
  2. תחביר מלא: `<React.Fragment>...</React.Fragment>`
  3. ייבוא מפורש: `<Fragment>...</Fragment>` (לאחר `import { Fragment } from 'react'`)
* **תכונות:** בתחביר המקוצר (`<>`) **אי אפשר** לשים תכונות כלל. בתחביר המלא ניתן לשים אך ורק את התכונה `key` (שימוש נפוץ בעת רינדור רשימות בלולאה).